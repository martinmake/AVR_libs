{
	using namespace Cabs::Colors;

	application.label_attr();
	application.border_attr();
	application.shadow_attr();
	application.selected_attr();
	application.screen_background_attr();
	application.widget_background_attr();
	// application.label_attr(RED_WHITE);
	// application.border_attr(BLACK_WHITE);
	// application.shadow_attr(BLACK_BLACK);
	// application.selected_attr();
	// application.screen_background_attr(BLUE_BLUE);
	// application.widget_background_attr(WHITE_WHITE);
	application.widget_gap(1);
}
